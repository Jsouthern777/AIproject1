public class Solver {
//same problem will be solved multiple times by both algorithms, so generate availabilities,
// numEmployees, numShifts, and startTime here
    public static boolean runInstance(final int numShifts, final int numEmployees, final int printCode){

        return false;
    }



    public static void main(String[] args) {
        runInstance(10,10, 1);
    }
}
